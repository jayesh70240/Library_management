<h1>Book List</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Checked Out</th>
      <th>Return</th>
      <th>Issued Date</th>
      <th>Due Date</th>

    </tr>
  </thead>
  <tbody>
  <% if @books.present? %>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.checked_out ? 'Yes' : 'No' %></td>
          <% if book.checked_out %>
            <td><%= link_to "Return", return_book_issue_path(book.id), method: :put, data: { confirm: 'Are you sure you want to return this book?' }, id: "return_#{book.id}"%></td>
            <td><%= book.book_issues&.last&.issued_date %></td>
            <td><%= book.book_issues&.last&.due_date %></td>
          <% else %>
            <td>N/A</td>
            <td>N/A</td>
            <td>N/A</td>
          <% end %>
      </tr>
    <% end %>
  <% else %>
    <tr>
      <td colspan="3">No books found.</td>
    </tr>
  <% end %>
</tbody>
</table>